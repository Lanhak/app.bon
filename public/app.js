function show(id){document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));const p=document.getElementById(id);if(p)p.classList.add('active')}
async function checkKey(){const key=document.getElementById('keyInput').value.trim();const device=document.getElementById('deviceInput').value.trim()||'DEVICE';const out=document.getElementById('keyResult');if(!key){out.textContent='Vui lòng nhập Key';return}try{const r=await fetch('/checkkey/api/check_date_key.php?APIKey='+encodeURIComponent(key)+'&device_id_local='+encodeURIComponent(device));out.textContent=await r.text()}catch(e){out.textContent='Không kết nối được server'}}
async function loadStats(){const box=document.getElementById('statsBox');try{const r=await fetch('/statistics');box.textContent=await r.text()}catch(e){box.textContent='Không tải được thống kê'}}
